#include <stdio.h>

void tri(int *a, int n){
    int s=0;
    for(int i=1;i<=n;i++){
        s += i;
        a[i-1] = s;
    }
}

int main(){
    int n=15;
    int a[50];

    tri(a, n);

    printf("Triangular numbers:\n");
    for(int i=0;i<n;i++){
        printf("%2d: %d\n", i+1, a[i]);
    }
}
